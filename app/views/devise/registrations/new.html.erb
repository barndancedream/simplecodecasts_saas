<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
  <% if params[:plan] == '2' %>
  <h1>Pro Account</h1>
  <p>Sign up for a pro account for $10/month to get all the stuff.</p>
  </div>
  <% else %>
  <div class="col-md-6 col-md-offset-3 text-center">
  <h1>Basic Account</h1>
  <p>Or you could get very little with this free account maybe.</p>
  </div>
  
  <% end %>
  </div>
    
    
  <div class="col-md-6 col-md-offset-4">
    <div class="well">

<h2>Sign up</h2>


<% if params[:plan] == '2' %>

<%= render "pro_form" %>

<% else %>

<%= render "basic_form" %>

<% end %>

<%= render "devise/shared/links" %>

</div>
</div>
</div>