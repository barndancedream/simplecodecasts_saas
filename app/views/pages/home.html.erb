<%= link_to "Create your profile!", new_user_profile_path(current_user) if current_user %> 

<div class="jumbotron">
    <h1>Welcome to RapMatch</h1>
    <h3>Your trusted source connecting producers and aspiring guest verse spitters since 2016.</h3>
</div>
<div class="row">
    <% if user_signed_in? %>
    <div class="col-md-6">
    <div class="well">
        <h2 class="text-center">The Community</h2>
        <% if current_user.profile %>
        <%= link_to "Edit Your Profile", "#", class: 'btn btn-default btn-lg btn-block' %>
        <%= link_to "View your Profile", user_path(current_user), class: 'btn btn-default btn-lg btn-block' %>
        
        <% else %>
        
    <%= link_to "Create your profile!", class: 'btn btn-default btn-lg btn-block' %>
    <% end %>
    </div>
    </div>
      <div class="col-md-6">
    <div class="well">
        <h2 class="text-center">The Community
        </h2>
        <p>Visit the community to visit other like-minded rappers and producers.</p>
        <%= link_to "Visit the community", "#", class: 'btn btn-default btn-lg btn-block' %>
    </div>
    </div>
    <% else %>
    <div class="col-md-6">
        <div class="well">
            <h2 class="text-center">Basic Plan Membership</h2>
            <h4>Sign up for free!</h4>
            <%= link_to "Sign up Free", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
        </div>
    </div>
    <div class="col-md-6">
        <div class="well">
            <h2 class="text-center">Pro Membership</h2>
            <h4>Get full access here!</h4>
            <%= link_to "Sign up Pro", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg btn-block' %>
        </div>
    </div>
    <% end %>
</div>